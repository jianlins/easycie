package edu.utah.bmi.nlp.uima.ae;

/**
 * This is an empty interface, simple used to indicate which class is designed for bunch inference.
 * common static fields are not defined in it, so that each class's static field can operate isolated
 * without interfere with each other.
 */
public interface BunchInferInterface {

}
